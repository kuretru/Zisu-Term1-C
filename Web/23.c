#include <stdio.h>
int main()
{
	int i, n;
	int weight[5] = {1, 3, 9, 27, 81}, used[5] = {0};
	scanf("%d", &n);
	for(i = 0; i < n; i++)
	{
		int obj, left, right = 0;
		scanf("%d", &obj);
		left = obj;
	}
	return 0;
}
